apiVersion: v1
kind: Service
metadata:
  name: {{ template "challenger.fullname" . }}
  labels:
    app: {{ template "challenger.name" . }}
    chart: {{ template "challenger.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "challenger.name" . }}
    release: {{ .Release.Name }}
